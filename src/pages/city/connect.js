// import {sendAsyncDataToCarts} from '../../../actions/xrractions/actionCreator'
export const mapStateToProps = (state)=>({      
       cityHot:state.city.cityHot,
       cityList:state.city.cityList
})

export const mapDispatchToProps = (dispatch)=>({     
      
})